{
  "name": "programmer-network-ajv",
  "description": "A JSON Schema Validator",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-errors": "^3.0.0",
    "ajv-formats": "^2.1.1",
    "esbuild-jest": "^0.5.0",
    "jest": "^29.5.0"
  }
}
