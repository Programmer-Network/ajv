{
  "name": "programmer-network-ajv",
  "description": "A JSON Schema Validator",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch"
  },
  "devDependencies": {
    "esbuild-jest": "^0.5.0",
    "jest": "^29.5.0"
  }
}
